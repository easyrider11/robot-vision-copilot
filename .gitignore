.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
*.egg-info/

# rollout artifacts: regenerate with `make demo-libero`
runs/*/
!runs/.gitkeep

# generated by `make audit`
docs/00-environment-audit.json

.DS_Store
.env

# LIBERO: vendored clone + generated config (make setup-libero recreates both)
external/LIBERO/
external/.libero-config/
external/libero-datasets/

# learned detector weights + report: regenerate with `make yolo` (~2 min on M3)
models/

# ultralytics downloads the base checkpoint into the CWD
yolo11n.pt

# colima/docker state, ROS build products
ros2_ws/build/
ros2_ws/install/
ros2_ws/log/

# personal interview-prep notes: kept local on purpose (not part of the public repo)
docs/07-interview-prep.md
